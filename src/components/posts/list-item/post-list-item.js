import { connect } from 'react-redux';
import PostListItemComponent from './post-list-item-component';

const mapStateToProps = (state, ownProps) => ({
});

export default connect(mapStateToProps)(PostListItemComponent);
